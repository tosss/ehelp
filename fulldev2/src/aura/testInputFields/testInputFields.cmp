<aura:component implements="force:appHostable">
    <aura:attribute name="contactRecord" type="Contact" default="{'sobjectType': 'Contact'}"/>
    <force:inputField aura:id="contactName" value="{!v.contactRecord.Name}"/>
</aura:component>